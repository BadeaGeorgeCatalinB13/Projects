README

This is a simple TCP server created using Python's `socket` module. It listens for incoming connections from clients and sends a welcome message to the connected client.

### Requirements
- Python 3.x
- No additional libraries are needed since it uses Python's built-in `socket` module.

### How to Run
1. Save the server code into a Python file, e.g., `tcp_server.py`.
2. Open a terminal/command prompt and navigate to the directory where the file is saved.
3. Run the server script with the following command:
